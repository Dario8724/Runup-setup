<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <EditText
            android:id="@+id/edtNome"
            android:hint="Nome da rota"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPersonName" />

        <RadioGroup
            android:id="@+id/rgTipo"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp">
            <RadioButton
                android:id="@+id/rbCorrida"
                android:text="Corrida" />
            <RadioButton
                android:id="@+id/rbCaminhada"
                android:text="Caminhada" />
        </RadioGroup>

        <Spinner
            android:id="@+id/spnDistancia"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp" />

        <CheckBox
            android:id="@+id/chkArvores"
            android:text="Mais árvores" />
        <CheckBox
            android:id="@+id/chkPraia"
            android:text="Perto da praia" />
        <CheckBox
            android:id="@+id/chkParque"
            android:text="Perto de parque" />
        <CheckBox
            android:id="@+id/chkSol"
            android:text="Rota ensolarada" />
        <CheckBox
            android:id="@+id/chkSubidas"
            android:text="Evitar subidas" />

        <Button
            android:id="@+id/btnGerar"
            android:text="Gerar rota"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/teal_700"
            android:textColor="@android:color/white" />
    </LinearLayout>
</ScrollView>
