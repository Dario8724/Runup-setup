<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔹 Permissões necessárias -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunupSetup"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 🔹 Chave da API do Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDhni-9ZSCjmR3rZp0FB5RcSTOKQqqKmso" />

        <!-- 🔹 Tela principal -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 Tela de filtros de rota -->
        <activity
            android:name=".RouteFiltersActivity"
            android:exported="false"
            android:label="Filtros de Rota"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- 🔹 Tela de rota (visualização estática, se ainda usada) -->
        <activity
            android:name=".ui.RouteActivity"
            android:exported="false"
            android:label="Gerar Rota"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- 🔹 Tela de corrida em tempo real -->
        <activity
            android:name=".ui.RunMapActivity"
            android:exported="false"
            android:label="Corrida em Tempo Real"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>
